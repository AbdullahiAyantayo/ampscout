{
  "version": 2,
  "buildCommand": "pip install poetry && poetry install",
  "installCommand": "pip install poetry && poetry install",
  "framework": "fastapi",
  "outputDirectory": ".",
  "devCommand": "poetry run uvicorn main:app --host 0.0.0.0 --port $PORT",
  "build": {
    "env": {
      "PYTHON_VERSION": "3.9.18"
    }
  },
  "python": {
    "version": "3.9.18"
  }
} 